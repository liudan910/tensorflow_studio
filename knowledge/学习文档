http://www.cnblogs.com/zhouyang209117/p/6517684.html
1.softmax归一化函数

-----------
1.onehot标签 一个长度为n的数组，只且个元素是1.0， 其他元素是0.0
如n=4时，
标签2对应的onehot标签是 0.0 0.0 1.0 0.0

使用onehot原因： 多分类cnn输出通常是softmax层，而它的输出是一个概率分布，从而 要求输入标签也概率分布的形式，进而算交叉熵之类